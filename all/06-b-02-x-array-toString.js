/**
 * Array.toString()
 * Method yang akan mengembalikan sebuah string
 * yang berisi representasi dari setiap elemen dalam array,
 * dipisahkan oleh tanda koma (,).
 * 
 */

const numbers = [1, 2, 3, 4];

console.info(numbers.toString()); // Output: "1,2,3,4"
