/**
 * Array
 * ---> Tipe data di JavaScript
 *      yang digunakan untuk menyimpan sekumpulan nilai
 *      atau elemen yang terkait
 * Array terdapat beberapa methode : 
 * - array.push() :
 *   --->   Metode untuk menambah elemen baru ke akhir array
 * 
 * - array.pull() :
 *   --->   Metode untuk menghapus elemen terakhir dari array
 * 
 * - array.shift() : 
 *   --->   Metode untuk menghapus elemen pertama dari array
 * 
 * - array.unshift() :
 *   --->   Metode untuk menambah elemen baru ke awal array
 * - array.splice() :
 *   --->   Methode untuk menambah
 *          atau menghapus elemen di posisi tertentu dalam array
 * - array.slice() :
 *   --->   Method untuk mengambil sebagian dari array
Array.concat(): menggabungkan dua atau lebih array menjadi satu array baru.
Array.join(): menggabungkan semua elemen array menjadi sebuah string dengan pemisah yang ditentukan.
Array.pop(): menghapus elemen terakhir dari array dan mengembalikan elemen tersebut.
Array.push(): menambah elemen baru ke akhir array dan mengembalikan panjang array setelah elemen ditambahkan.
Array.reverse(): membalikkan urutan elemen dalam array.
Array.shift(): menghapus elemen pertama dari array dan mengembalikan elemen tersebut.
Array.slice(): mengambil sebagian dari array dan mengembalikan sebuah array baru.
Array.sort(): mengurutkan elemen dalam array.
Array.splice(): menambah atau menghapus elemen dari array pada posisi tertentu.
Array.unshift(): menambah elemen baru ke awal array dan mengembalikan panjang array setelah elemen ditambahkan.
 * 
 * 
 * 
 * 
 */

const fruits = ['apple', 'banana', 'orange'];
console.info(fruits);

const buah = new Array('apel', 'pisang', 'jeruk');
console.info(buah);