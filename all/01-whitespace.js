/**
 * Whitespace
 * ---> Dapat digunakan untuk meningkatkan legibility (kelihatan) kode program
 *      dengan mengelompokkan statement-statement dan mengatur layout kode secara visual.
 *      Namun, JavaScript tidak memerlukan penggunaan whitespace secara eksplisit
 *      untuk memisahkan statement-statement atau blok kode.
 * 
 */

// Tanpa whitespace 
let formattedNotWhiteSpace = true; if (formattedNotWhiteSpace) { console.info('The code is easy to read'); }

// Dengan whitespace
let formattedWhiteSpace = true;

if (formattedWhiteSpace) {
    console.info('The code is easy to read');
}